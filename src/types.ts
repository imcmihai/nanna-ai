export interface FormData {
  email: string;
  name: string;
  company: string;
  howHeard: string;
  acceptTerms: boolean;
}

export interface Integration {
  name: string;
  logo: React.ReactNode;
}